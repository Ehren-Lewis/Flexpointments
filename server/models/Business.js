const { Schema, model, Types } = require("mongoose");

const businessSchema = new Schema({

})



// preseented a form.

// Business name

// owner / manager? 
    // This can also be a separate document with roles. 




// Employee which will reference roles.
// EMployee has 2 subdocs, one is roles, the other is customers